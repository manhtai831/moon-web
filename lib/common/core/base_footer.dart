import 'package:flutter/material.dart';

class BaseFooter extends StatelessWidget {
  const BaseFooter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
